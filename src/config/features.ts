import type { FeatureFlags } from '../types/content';

export const features: FeatureFlags = {
  projects: true,
  projectDetailPages: false,
  team: false,
  publications: true,
  news: true,
  resources: false,
  multilingual: false,
  analytics: false,
};
